debug: false

preprocessors:
  - type: feature_flags
    config:
      file: flags.yaml # relative to this file
      keys: [new_ui]

program:
  version: 1
  instructions:
    - op: set
      key: was_here
      value: true
    - op: merge


source:
  type: stdin

output:
  type: stdout
  config:
    pretty: true